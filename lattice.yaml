type: v1/system

node_pools:
  main:
    instance_type: t2.large
    num_instances: 1

components:
  www:
    type: v1/reference

    git_repository:
      url: https://github.com/gregbanks/static-http.git
      tag: 1.3.0
      file: definition/www.yaml

    parameters:
      node_pool: /:main
      num_instances: 1
